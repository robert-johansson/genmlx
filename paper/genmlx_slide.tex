\documentclass[aspectratio=1610]{beamer}
\usetheme{metropolis}
\usepackage{fontspec}
\usepackage{booktabs}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning,fit,backgrounds}

\title{GenMLX}
\subtitle{Probabilistic Programming with Swappable Inference Handlers}
\author{}
\date{}

\begin{document}
\begin{frame}[t]{GenMLX: The Handler is the Insight}

\begin{columns}[T]
\begin{column}{0.50\textwidth}

\textbf{Core idea:} Separate the model from the inference strategy.

\smallskip

The gen function defines \emph{what} to compute.\\
The handler defines \emph{how} --- and it's swappable.

\begin{center}
\begin{tikzpicture}[
    node distance=0.3cm and 0.6cm,
    box/.style={draw, rounded corners, minimum height=0.55cm,
                minimum width=2.3cm, font=\footnotesize, align=center},
    handler/.style={box, fill=mLightBrown!30},
    arrow/.style={-{Stealth[length=2mm]}, thick}
]
    \node[box, fill=mLightGreen!30] (gen) {gen function};
    \node[handler, below=0.3cm of gen] (h1) {Sample handler};
    \node[handler, below=0.12cm of h1] (h2) {Kalman handler};
    \node[handler, below=0.12cm of h2] (h3) {EKF handler};
    \node[handler, below=0.12cm of h3] (h4) {Batched handler};

    \node[right=0.4cm of h1, font=\footnotesize\itshape, text=gray] {approximate};
    \node[right=0.4cm of h2, font=\footnotesize\itshape, text=gray] {exact};
    \node[right=0.4cm of h3, font=\footnotesize\itshape, text=gray] {linearized};
    \node[right=0.4cm of h4, font=\footnotesize\itshape, text=gray] {N particles};

    \draw[arrow] (gen) -- (h1);
    \draw[arrow, densely dashed, gray] (gen.south) ++(0,0) -- (h2.north);
    \draw[arrow, densely dashed, gray] (gen.south) ++(0,0) -- (h3.north);
    \draw[arrow, densely dashed, gray] (gen.south) ++(0,0) -- (h4.north);

    \begin{scope}[on background layer]
        \node[fit=(h1)(h2)(h3)(h4), draw=mDarkTeal, dashed,
              rounded corners, inner sep=3pt,
              label={[font=\tiny,text=mDarkTeal]below:compose via \texttt{wrap-analytical}}] {};
    \end{scope}
\end{tikzpicture}
\end{center}

\end{column}
\begin{column}{0.48\textwidth}

\textbf{What this enables:}

\smallskip
\small
\begin{tabular}{@{}ll@{}}
\toprule
\textbf{Capability} & \textbf{Speedup} \\
\midrule
Shape-based batching & 1703$\times$ \\
Batched Unfold & 80$\times$ \\
Batched Switch/Mix & 6--19$\times$ \\
Compiled MCMC & 10.9$\times$ \\
Analytical middleware & exact \\
\bottomrule
\end{tabular}

\smallskip

\small\textbf{Beyond Gen.jl/GenJAX:}
\begin{itemize}\itemsep0pt \parskip0pt
    \item Compose Kalman + EKF + HMM + conjugate
    \item Batch combinators (not just flat models)
    \item Swap inference without changing model code
\end{itemize}

\vfill

\scriptsize\color{gray}
10.8K lines ClojureScript \,$\cdot$\, 27 distributions \,$\cdot$\, 10 combinators \,$\cdot$\, 21 inference algorithms\\
GPU via MLX (Metal + CUDA) \,$\cdot$\, purely functional \,$\cdot$\, 302 checks vs Gen.jl \& GenJAX

\end{column}
\end{columns}

\end{frame}
\end{document}
