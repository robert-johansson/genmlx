node_modules/
.nrepl-port
.cpcache/
.nbb/
*.pdf

# Environment / secrets
.env

# ARC-AGI (feature/arc-agi-3)
environment_files/

# Claude Code
.claude/
.mcp.json

# Python virtualenvs
.venv-genjax/

# shadow-cljs
.shadow-cljs/
out/

# Local scratch (review outputs, ephemeral notes — kept out of the tree)
tmp/

# Agents-book figure pipeline: intermediate data (regenerated by bin/agents-book-figures;
# only the rendered PNGs/GIFs under docs/agents/src/figures/ are committed)
docs/agents/render/data/

# OS
.DS_Store

# External repos (cloned for comparison)
genjax/
genstudio/
# Only the external comparison clones under bench/ — NOT the whole dir, or new
# bench/*.cljs scripts get silently untracked (genmlx-yo0d).
bench/genjax/
bench/genjl/


# Python
__pycache__/
cross_system_tests/results/

# Corpus training artifacts (local-only)
corpus/adapters/*.safetensors
corpus/train.jsonl
corpus/valid.jsonl
corpus/training_pairs.jsonl

.beans/
.beans.yml
beans.md
