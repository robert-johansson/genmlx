\documentclass[border=14pt,tikz]{standalone}
\usepackage{fontspec}
\setmainfont{Helvetica Neue}
\setsansfont{Helvetica Neue}
\setmonofont{Menlo}
\usepackage{tikz}
\usetikzlibrary{positioning, arrows.meta, decorations.pathreplacing}

\definecolor{handler}{HTML}{FFF3E0}
\definecolor{handlerborder}{HTML}{FF9800}
\definecolor{batch}{HTML}{E8F5E9}
\definecolor{batchborder}{HTML}{4CAF50}
\definecolor{accent}{HTML}{7B1FA2}

\begin{document}
\begin{tikzpicture}[
    >=Stealth,
    node distance=0.6cm and 0.5cm,
    hbox/.style={draw, rounded corners=3pt, inner sep=5pt,
                 font=\sffamily\scriptsize, fill=handler, draw=handlerborder, line width=0.7pt},
    batchbox/.style={draw, rounded corners=3pt, inner sep=10pt,
                     font=\sffamily\small, fill=batch, draw=batchborder, line width=0.8pt, align=center},
    title/.style={font=\sffamily\small\bfseries},
    annot/.style={font=\sffamily\scriptsize, text=black!50},
]

% ======== SCALAR ========
\node[title] (st) {Scalar: $N$ separate executions};

\node[hbox, below=0.5cm of st] (h1) {handler};
\node[hbox, right=of h1] (h2) {handler};
\node[hbox, right=of h2] (h3) {handler};
\node[annot, right=0.3cm of h3] {$\cdots$};
\node[hbox, right=0.8cm of h3] (hN) {handler};

\node[annot, below=0.5cm of h2] (sn) {$N$ calls $\times$ $N$ dispatches};

% Brace
\draw[decorate, decoration={brace, amplitude=5pt, mirror}, line width=0.6pt]
    ([yshift=-2pt]h1.south west) -- ([yshift=-2pt]hN.south east)
    node[midway, below=7pt, annot] {$N$ particles};

% ======== VECTORIZED ========
\node[title, below=2.8cm of st] (vt) {Vectorized: 1 execution, $[N]$-shaped arrays};

\node[hbox, below=0.5cm of vt, inner sep=8pt, font=\sffamily\small]
    (hvec) {handler (once)};

\node[batchbox, below=0.6cm of hvec]
    (bvec) {$[N]$-shaped result};

\draw[->, line width=1pt, batchborder] (hvec) -- (bvec);

\node[annot, right=1cm of hvec, align=left] {%
    Same handler code\\[2pt]
    {\ttfamily\tiny dist-sample-n} instead of {\ttfamily\tiny dist-sample}\\[2pt]
    MLX broadcasting handles the rest};

% Speedup arrow
\draw[->, line width=1.5pt, accent]
    ([xshift=-2.5cm]st.south) -- ([xshift=-2.5cm]vt.north)
    node[midway, left, font=\sffamily\scriptsize\bfseries, accent, align=right]
    {$352$--$1520\times$\\speedup};

\end{tikzpicture}
\end{document}
