;; Frozen LLM-authored programs for the synthesis-Occam experiment (genmlx-heaw).
;; Generated by bench/synthesis_occam_generate.cljs against qwen3-0.6b base (knowledge mode).
;; DO NOT hand-edit: regenerate with the generator to refresh provenance.
;; Each entry is one LLM-synthesized model; the scorer re-evals :code and
;; recomputes fire/exact/IS deterministically.

[{:description "A thermometer measures a fixed but unknown room temperature with noise. Write a model with a latent variable `mu` (the true temperature in Celsius) and an observation `y` (the noisy reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 25 2)", :y "(dist/gaussian mu 0.1)"}, :task-id :temperature, :eval-ok? true, :score-method "exact", :observations {:y 21.5}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4200, :raw-text "mu ~ gaussian(25, 2)\ny ~ gaussian(mu, 0.1)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 25 2))\n        y (trace :y (dist/gaussian mu 0.1))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "A thermometer measures a fixed but unknown room temperature with noise. Write a model with a latent variable `mu` (the true temperature in Celsius) and an observation `y` (the noisy reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 25 1)", :y "(dist/exponential 0.1)"}, :task-id :temperature, :eval-ok? true, :score-method "hmc", :observations {:y 21.5}, :obs-depends? false, :latent :mu, :variables [:mu :y], :seed 4201, :raw-text "mu ~ gaussian(25, 1)\ny ~ exponential(0.1)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 25 1))\n        y (trace :y (dist/exponential 0.1))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? false} {:description "A thermometer measures a fixed but unknown room temperature with noise. Write a model with a latent variable `mu` (the true temperature in Celsius) and an observation `y` (the noisy reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 25 3)", :y "(dist/gaussian mu 1)"}, :task-id :temperature, :eval-ok? true, :score-method "exact", :observations {:y 21.5}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4202, :raw-text "mu ~ gaussian(25, 3)\ny ~ gaussian(mu, 1)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 25 3))\n        y (trace :y (dist/gaussian mu 1))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "A thermometer measures a fixed but unknown room temperature with noise. Write a model with a latent variable `mu` (the true temperature in Celsius) and an observation `y` (the noisy reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 25 1.5)", :y "(dist/gaussian mu 0.1)"}, :task-id :temperature, :eval-ok? true, :score-method "exact", :observations {:y 21.5}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4203, :raw-text "mu ~ gaussian(25, 1.5)\ny ~ gaussian(mu, 0.1)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 25 1.5))\n        y (trace :y (dist/gaussian mu 0.1))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "A thermometer measures a fixed but unknown room temperature with noise. Write a model with a latent variable `mu` (the true temperature in Celsius) and an observation `y` (the noisy reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 15 1.5)", :y "(dist/gaussian mu 0.3)"}, :task-id :temperature, :eval-ok? true, :score-method "exact", :observations {:y 21.5}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4204, :raw-text "mu ~ gaussian(15, 1.5)\ny ~ gaussian(mu, 0.3)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 15 1.5))\n        y (trace :y (dist/gaussian mu 0.3))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "A kitchen scale reports a noisy weight. Write a model with a latent `mu` (the true weight in grams) and an observation `y` (the scale reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 80 5)", :y "(dist/gaussian mu 3)"}, :task-id :scale-weight, :eval-ok? true, :score-method "exact", :observations {:y 503}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4205, :raw-text "mu ~ gaussian(80, 5)\ny ~ gaussian(mu, 3)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 80 5))\n        y (trace :y (dist/gaussian mu 3))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "A kitchen scale reports a noisy weight. Write a model with a latent `mu` (the true weight in grams) and an observation `y` (the scale reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 0 5)", :y "(dist/gaussian mu 0.1)"}, :task-id :scale-weight, :eval-ok? true, :score-method "exact", :observations {:y 503}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4206, :raw-text "mu ~ gaussian(0, 5)\ny ~ gaussian(mu, 0.1)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 0 5))\n        y (trace :y (dist/gaussian mu 0.1))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "A kitchen scale reports a noisy weight. Write a model with a latent `mu` (the true weight in grams) and an observation `y` (the scale reading). `y` must depend on `mu`.", :dist-map {}, :task-id :scale-weight, :eval-ok? true, :score-method "hmc", :observations {:y 503}, :obs-depends? false, :latent :mu, :variables [:mu :y], :seed 4207, :raw-text "mu ~ gaussian(60, 5)  \ny ~ gaussian(mu, 2)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 0 1))\n        y (trace :y (dist/gaussian 0 1))]\n    {:mu mu :y y}))", :parsed? false, :obs-addr :y, :fired? false} {:description "A kitchen scale reports a noisy weight. Write a model with a latent `mu` (the true weight in grams) and an observation `y` (the scale reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 30 5)", :y "(dist/gaussian mu 2)"}, :task-id :scale-weight, :eval-ok? true, :score-method "exact", :observations {:y 503}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4208, :raw-text "mu ~ gaussian(30, 5)\ny ~ gaussian(mu, 2)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 30 5))\n        y (trace :y (dist/gaussian mu 2))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "A kitchen scale reports a noisy weight. Write a model with a latent `mu` (the true weight in grams) and an observation `y` (the scale reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 10 3)", :y "(dist/gaussian mu 2)"}, :task-id :scale-weight, :eval-ok? true, :score-method "exact", :observations {:y 503}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4209, :raw-text "mu ~ gaussian(10, 3)\ny ~ gaussian(mu, 2)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 10 3))\n        y (trace :y (dist/gaussian mu 2))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "An ultrasonic sensor measures distance with noise. Write a model with a latent `mu` (the true distance in centimeters) and an observation `y` (the sensor reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 10 1)", :y "(dist/gaussian mu 0.5)"}, :task-id :distance-sensor, :eval-ok? true, :score-method "exact", :observations {:y 147.2}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4210, :raw-text "mu ~ gaussian(10, 1)\ny ~ gaussian(mu, 0.5)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 10 1))\n        y (trace :y (dist/gaussian mu 0.5))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "An ultrasonic sensor measures distance with noise. Write a model with a latent `mu` (the true distance in centimeters) and an observation `y` (the sensor reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 0 10)", :y "(dist/exponential 0.003)"}, :task-id :distance-sensor, :eval-ok? true, :score-method "hmc", :observations {:y 147.2}, :obs-depends? false, :latent :mu, :variables [:mu :y], :seed 4211, :raw-text "mu ~ gaussian(0, 10)\ny ~ exponential(0.003)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 0 10))\n        y (trace :y (dist/exponential 0.003))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? false} {:description "An ultrasonic sensor measures distance with noise. Write a model with a latent `mu` (the true distance in centimeters) and an observation `y` (the sensor reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 70 10)", :y "(dist/gaussian mu 5)"}, :task-id :distance-sensor, :eval-ok? true, :score-method "exact", :observations {:y 147.2}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4212, :raw-text "mu ~ gaussian(70, 10)\ny ~ gaussian(mu, 5)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 70 10))\n        y (trace :y (dist/gaussian mu 5))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "An ultrasonic sensor measures distance with noise. Write a model with a latent `mu` (the true distance in centimeters) and an observation `y` (the sensor reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 0 1)", :y "(dist/gaussian mu 0.1)"}, :task-id :distance-sensor, :eval-ok? true, :score-method "exact", :observations {:y 147.2}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4213, :raw-text "mu ~ gaussian(0, 1)\ny ~ gaussian(mu, 0.1)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 0 1))\n        y (trace :y (dist/gaussian mu 0.1))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "An ultrasonic sensor measures distance with noise. Write a model with a latent `mu` (the true distance in centimeters) and an observation `y` (the sensor reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 10 5)", :y "(dist/gaussian mu 2)"}, :task-id :distance-sensor, :eval-ok? true, :score-method "exact", :observations {:y 147.2}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4214, :raw-text "mu ~ gaussian(10, 5)\ny ~ gaussian(mu, 2)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 10 5))\n        y (trace :y (dist/gaussian mu 2))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "A student has a latent ability and produces a noisy test score. Write a model with a latent `mu` (the ability) and an observation `y` (the observed score). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 0 10)", :y "(dist/gaussian mu 2)"}, :task-id :test-score, :eval-ok? true, :score-method "exact", :observations {:y 88}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4215, :raw-text "mu ~ gaussian(0, 10)\ny ~ gaussian(mu, 2)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 0 10))\n        y (trace :y (dist/gaussian mu 2))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "A student has a latent ability and produces a noisy test score. Write a model with a latent `mu` (the ability) and an observation `y` (the observed score). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 70 10)", :y "(dist/gaussian mu 2)"}, :task-id :test-score, :eval-ok? true, :score-method "exact", :observations {:y 88}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4216, :raw-text "mu ~ gaussian(70, 10)\ny ~ gaussian(mu, 2)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 70 10))\n        y (trace :y (dist/gaussian mu 2))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "A student has a latent ability and produces a noisy test score. Write a model with a latent `mu` (the ability) and an observation `y` (the observed score). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 50 10)", :y "(dist/gaussian mu 2)"}, :task-id :test-score, :eval-ok? true, :score-method "exact", :observations {:y 88}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4217, :raw-text "mu ~ gaussian(50, 10)\ny ~ gaussian(mu, 2)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 50 10))\n        y (trace :y (dist/gaussian mu 2))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "A student has a latent ability and produces a noisy test score. Write a model with a latent `mu` (the ability) and an observation `y` (the observed score). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 30 5)", :y "(dist/gaussian mu 10)"}, :task-id :test-score, :eval-ok? true, :score-method "exact", :observations {:y 88}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4218, :raw-text "mu ~ gaussian(30, 5)\ny ~ gaussian(mu, 10)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 30 5))\n        y (trace :y (dist/gaussian mu 10))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "A student has a latent ability and produces a noisy test score. Write a model with a latent `mu` (the ability) and an observation `y` (the observed score). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 70 10)", :y "(dist/gaussian mu 2)"}, :task-id :test-score, :eval-ok? true, :score-method "exact", :observations {:y 88}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4219, :raw-text "mu ~ gaussian(70, 10)\ny ~ gaussian(mu, 2)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 70 10))\n        y (trace :y (dist/gaussian mu 2))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "A voltmeter reads a fixed voltage with measurement noise. Write a model with a latent `mu` (the true voltage) and an observation `y` (the meter reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 50 10)", :y "(dist/gaussian mu 2)"}, :task-id :voltage-meter, :eval-ok? true, :score-method "exact", :observations {:y 5.1}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4220, :raw-text "mu ~ gaussian(50, 10)\ny ~ gaussian(mu, 2)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 50 10))\n        y (trace :y (dist/gaussian mu 2))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "A voltmeter reads a fixed voltage with measurement noise. Write a model with a latent `mu` (the true voltage) and an observation `y` (the meter reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 0 1)", :y "(dist/gaussian mu 0.05)"}, :task-id :voltage-meter, :eval-ok? true, :score-method "exact", :observations {:y 5.1}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4221, :raw-text "mu ~ gaussian(0, 1)\ny ~ gaussian(mu, 0.05)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 0 1))\n        y (trace :y (dist/gaussian mu 0.05))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "A voltmeter reads a fixed voltage with measurement noise. Write a model with a latent `mu` (the true voltage) and an observation `y` (the meter reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 0 1)", :y "(dist/gaussian mu 0.1)"}, :task-id :voltage-meter, :eval-ok? true, :score-method "exact", :observations {:y 5.1}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4222, :raw-text "mu ~ gaussian(0, 1)\ny ~ gaussian(mu, 0.1)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 0 1))\n        y (trace :y (dist/gaussian mu 0.1))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "A voltmeter reads a fixed voltage with measurement noise. Write a model with a latent `mu` (the true voltage) and an observation `y` (the meter reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 50 5)", :y "(dist/gaussian mu 2)"}, :task-id :voltage-meter, :eval-ok? true, :score-method "exact", :observations {:y 5.1}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4223, :raw-text "mu ~ gaussian(50, 5)\ny ~ gaussian(mu, 2)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 50 5))\n        y (trace :y (dist/gaussian mu 2))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "A voltmeter reads a fixed voltage with measurement noise. Write a model with a latent `mu` (the true voltage) and an observation `y` (the meter reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 0 1)", :y "(dist/gaussian mu 0.01)"}, :task-id :voltage-meter, :eval-ok? true, :score-method "exact", :observations {:y 5.1}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4224, :raw-text "mu ~ gaussian(0, 1)\ny ~ gaussian(mu, 0.01)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 0 1))\n        y (trace :y (dist/gaussian mu 0.01))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "A GPS unit reports a noisy position coordinate. Write a model with a latent `mu` (the true coordinate) and an observation `y` (the GPS reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 0 1)", :y "(dist/exponential 0.1)"}, :task-id :gps-position, :eval-ok? true, :score-method "hmc", :observations {:y 12.7}, :obs-depends? false, :latent :mu, :variables [:mu :y], :seed 4225, :raw-text "mu ~ gaussian(0, 1)\ny ~ exponential(0.1)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 0 1))\n        y (trace :y (dist/exponential 0.1))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? false} {:description "A GPS unit reports a noisy position coordinate. Write a model with a latent `mu` (the true coordinate) and an observation `y` (the GPS reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 10 5)", :y "(dist/gaussian mu 2)"}, :task-id :gps-position, :eval-ok? true, :score-method "exact", :observations {:y 12.7}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4226, :raw-text "mu ~ gaussian(10, 5)\ny ~ gaussian(mu, 2)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 10 5))\n        y (trace :y (dist/gaussian mu 2))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "A GPS unit reports a noisy position coordinate. Write a model with a latent `mu` (the true coordinate) and an observation `y` (the GPS reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 70 10)", :y "(dist/gaussian mu 2)"}, :task-id :gps-position, :eval-ok? true, :score-method "exact", :observations {:y 12.7}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4227, :raw-text "mu ~ gaussian(70, 10)\ny ~ gaussian(mu, 2)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 70 10))\n        y (trace :y (dist/gaussian mu 2))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? true} {:description "A GPS unit reports a noisy position coordinate. Write a model with a latent `mu` (the true coordinate) and an observation `y` (the GPS reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/gaussian 50 10)", :y "(dist/exponential 0.01)"}, :task-id :gps-position, :eval-ok? true, :score-method "hmc", :observations {:y 12.7}, :obs-depends? false, :latent :mu, :variables [:mu :y], :seed 4228, :raw-text "mu ~ gaussian(50, 10)\ny ~ exponential(0.01)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/gaussian 50 10))\n        y (trace :y (dist/exponential 0.01))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? false} {:description "A GPS unit reports a noisy position coordinate. Write a model with a latent `mu` (the true coordinate) and an observation `y` (the GPS reading). `y` must depend on `mu`.", :dist-map {:mu "(dist/uniform 0 100)", :y "(dist/exponential mu)"}, :task-id :gps-position, :eval-ok? true, :score-method "hmc", :observations {:y 12.7}, :obs-depends? true, :latent :mu, :variables [:mu :y], :seed 4229, :raw-text "mu ~ uniform(0, 100)\ny ~ exponential(mu)", :code "(fn [trace]\n  (let [mu (trace :mu (dist/uniform 0 100))\n        y (trace :y (dist/exponential mu))]\n    {:mu mu :y y}))", :parsed? true, :obs-addr :y, :fired? false} {:description "A coin has an unknown bias. Write a model with a latent `p` (the probability of heads) and an observation `y` (1 if the flip was heads, 0 otherwise). `y` must depend on `p`.", :dist-map {:p "(dist/bernoulli 0.5)", :y "(dist/bernoulli p)"}, :task-id :coin-bias, :eval-ok? true, :score-method "hmc", :observations {:y 1}, :obs-depends? true, :latent :p, :variables [:p :y], :seed 4230, :raw-text "p ~ bernoulli(0.5)\ny ~ bernoulli(p)", :code "(fn [trace]\n  (let [p (trace :p (dist/bernoulli 0.5))\n        y (trace :y (dist/bernoulli p))]\n    {:p p :y y}))", :parsed? true, :obs-addr :y, :fired? false} {:description "A coin has an unknown bias. Write a model with a latent `p` (the probability of heads) and an observation `y` (1 if the flip was heads, 0 otherwise). `y` must depend on `p`.", :dist-map {:p "(dist/bernoulli 0.5)", :y "(dist/bernoulli p)"}, :task-id :coin-bias, :eval-ok? true, :score-method "hmc", :observations {:y 1}, :obs-depends? true, :latent :p, :variables [:p :y], :seed 4231, :raw-text "p ~ bernoulli(0.5)\ny ~ bernoulli(p)", :code "(fn [trace]\n  (let [p (trace :p (dist/bernoulli 0.5))\n        y (trace :y (dist/bernoulli p))]\n    {:p p :y y}))", :parsed? true, :obs-addr :y, :fired? false} {:description "A coin has an unknown bias. Write a model with a latent `p` (the probability of heads) and an observation `y` (1 if the flip was heads, 0 otherwise). `y` must depend on `p`.", :dist-map {:p "(dist/bernoulli 0.5)", :y "(dist/bernoulli p)"}, :task-id :coin-bias, :eval-ok? true, :score-method "hmc", :observations {:y 1}, :obs-depends? true, :latent :p, :variables [:p :y], :seed 4232, :raw-text "p ~ bernoulli(0.5)\ny ~ bernoulli(p)", :code "(fn [trace]\n  (let [p (trace :p (dist/bernoulli 0.5))\n        y (trace :y (dist/bernoulli p))]\n    {:p p :y y}))", :parsed? true, :obs-addr :y, :fired? false} {:description "A coin has an unknown bias. Write a model with a latent `p` (the probability of heads) and an observation `y` (1 if the flip was heads, 0 otherwise). `y` must depend on `p`.", :dist-map {:p "(dist/bernoulli 0.5)", :y "(dist/bernoulli p)"}, :task-id :coin-bias, :eval-ok? true, :score-method "hmc", :observations {:y 1}, :obs-depends? true, :latent :p, :variables [:p :y], :seed 4233, :raw-text "p ~ bernoulli(0.5)\ny ~ bernoulli(p)", :code "(fn [trace]\n  (let [p (trace :p (dist/bernoulli 0.5))\n        y (trace :y (dist/bernoulli p))]\n    {:p p :y y}))", :parsed? true, :obs-addr :y, :fired? false} {:description "A coin has an unknown bias. Write a model with a latent `p` (the probability of heads) and an observation `y` (1 if the flip was heads, 0 otherwise). `y` must depend on `p`.", :dist-map {:p "(dist/bernoulli 0.5)", :y "(dist/bernoulli p)"}, :task-id :coin-bias, :eval-ok? true, :score-method "hmc", :observations {:y 1}, :obs-depends? true, :latent :p, :variables [:p :y], :seed 4234, :raw-text "p ~ bernoulli(0.5)\ny ~ bernoulli(p)", :code "(fn [trace]\n  (let [p (trace :p (dist/bernoulli 0.5))\n        y (trace :y (dist/bernoulli p))]\n    {:p p :y y}))", :parsed? true, :obs-addr :y, :fired? false} {:description "A factory line has an unknown defect probability. Write a model with a latent `p` (the defect probability) and an observation `y` (1 if a sampled item is defective). `y` must depend on `p`.", :dist-map {}, :task-id :defect-rate, :eval-ok? true, :score-method "hmc", :observations {:y 0}, :obs-depends? false, :latent :p, :variables [:p :y], :seed 4235, :raw-text "p ~ bernoulli(p)  \ny ~ bernoulli(p)", :code "(fn [trace]\n  (let [p (trace :p (dist/gaussian 0 1))\n        y (trace :y (dist/gaussian 0 1))]\n    {:p p :y y}))", :parsed? false, :obs-addr :y, :fired? false} {:description "A factory line has an unknown defect probability. Write a model with a latent `p` (the defect probability) and an observation `y` (1 if a sampled item is defective). `y` must depend on `p`.", :dist-map {}, :task-id :defect-rate, :eval-ok? true, :score-method "hmc", :observations {:y 0}, :obs-depends? false, :latent :p, :variables [:p :y], :seed 4236, :raw-text "p ~ bernoulli(0.1)  \ny ~ bernoulli(p)", :code "(fn [trace]\n  (let [p (trace :p (dist/gaussian 0 1))\n        y (trace :y (dist/gaussian 0 1))]\n    {:p p :y y}))", :parsed? false, :obs-addr :y, :fired? false} {:description "A factory line has an unknown defect probability. Write a model with a latent `p` (the defect probability) and an observation `y` (1 if a sampled item is defective). `y` must depend on `p`.", :dist-map {:p "(dist/bernoulli 0.1)", :y "(dist/bernoulli p)"}, :task-id :defect-rate, :eval-ok? true, :score-method "hmc", :observations {:y 0}, :obs-depends? true, :latent :p, :variables [:p :y], :seed 4237, :raw-text "p ~ bernoulli(0.1)\ny ~ bernoulli(p)", :code "(fn [trace]\n  (let [p (trace :p (dist/bernoulli 0.1))\n        y (trace :y (dist/bernoulli p))]\n    {:p p :y y}))", :parsed? true, :obs-addr :y, :fired? false} {:description "A factory line has an unknown defect probability. Write a model with a latent `p` (the defect probability) and an observation `y` (1 if a sampled item is defective). `y` must depend on `p`.", :dist-map {}, :task-id :defect-rate, :eval-ok? true, :score-method "hmc", :observations {:y 0}, :obs-depends? false, :latent :p, :variables [:p :y], :seed 4238, :raw-text "p ~ bernoulli(0.1)  \ny ~ Bernoulli(p)", :code "(fn [trace]\n  (let [p (trace :p (dist/gaussian 0 1))\n        y (trace :y (dist/gaussian 0 1))]\n    {:p p :y y}))", :parsed? false, :obs-addr :y, :fired? false} {:description "A factory line has an unknown defect probability. Write a model with a latent `p` (the defect probability) and an observation `y` (1 if a sampled item is defective). `y` must depend on `p`.", :dist-map {:p "(dist/bernoulli 0.5)", :y "(dist/bernoulli p)"}, :task-id :defect-rate, :eval-ok? true, :score-method "hmc", :observations {:y 0}, :obs-depends? true, :latent :p, :variables [:p :y], :seed 4239, :raw-text "p ~ bernoulli(0.5)\ny ~ bernoulli(p)", :code "(fn [trace]\n  (let [p (trace :p (dist/bernoulli 0.5))\n        y (trace :y (dist/bernoulli p))]\n    {:p p :y y}))", :parsed? true, :obs-addr :y, :fired? false} {:description "Customers arrive and the wait time between arrivals is random. Write a model with a latent `rate` (the arrival rate) and an observation `y` (an observed wait time). `y` must depend on `rate`.", :dist-map {:rate "(dist/exponential 1)", :y "(dist/exponential rate)"}, :task-id :wait-time, :eval-ok? true, :score-method "hmc", :observations {:y 3.4}, :obs-depends? true, :latent :rate, :variables [:rate :y], :seed 4240, :raw-text "rate ~ exponential(1)\ny ~ exponential(rate)", :code "(fn [trace]\n  (let [rate (trace :rate (dist/exponential 1))\n        y (trace :y (dist/exponential rate))]\n    {:rate rate :y y}))", :parsed? true, :obs-addr :y, :fired? false} {:description "Customers arrive and the wait time between arrivals is random. Write a model with a latent `rate` (the arrival rate) and an observation `y` (an observed wait time). `y` must depend on `rate`.", :dist-map {:rate "(dist/exponential 1)", :y "(dist/exponential rate)"}, :task-id :wait-time, :eval-ok? true, :score-method "hmc", :observations {:y 3.4}, :obs-depends? true, :latent :rate, :variables [:rate :y], :seed 4241, :raw-text "rate ~ exponential(1)\ny ~ exponential(rate)", :code "(fn [trace]\n  (let [rate (trace :rate (dist/exponential 1))\n        y (trace :y (dist/exponential rate))]\n    {:rate rate :y y}))", :parsed? true, :obs-addr :y, :fired? false} {:description "Customers arrive and the wait time between arrivals is random. Write a model with a latent `rate` (the arrival rate) and an observation `y` (an observed wait time). `y` must depend on `rate`.", :dist-map {:rate "(dist/exponential 1)", :y "(dist/gaussian rate 1)"}, :task-id :wait-time, :eval-ok? true, :score-method "hmc", :observations {:y 3.4}, :obs-depends? true, :latent :rate, :variables [:rate :y], :seed 4242, :raw-text "rate ~ exponential(1)\ny ~ gaussian(rate, 1)", :code "(fn [trace]\n  (let [rate (trace :rate (dist/exponential 1))\n        y (trace :y (dist/gaussian rate 1))]\n    {:rate rate :y y}))", :parsed? true, :obs-addr :y, :fired? false} {:description "Customers arrive and the wait time between arrivals is random. Write a model with a latent `rate` (the arrival rate) and an observation `y` (an observed wait time). `y` must depend on `rate`.", :dist-map {:rate "(dist/exponential 1)", :y "(dist/exponential rate)"}, :task-id :wait-time, :eval-ok? true, :score-method "hmc", :observations {:y 3.4}, :obs-depends? true, :latent :rate, :variables [:rate :y], :seed 4243, :raw-text "rate ~ exponential(1)\ny ~ exponential(rate)", :code "(fn [trace]\n  (let [rate (trace :rate (dist/exponential 1))\n        y (trace :y (dist/exponential rate))]\n    {:rate rate :y y}))", :parsed? true, :obs-addr :y, :fired? false} {:description "Customers arrive and the wait time between arrivals is random. Write a model with a latent `rate` (the arrival rate) and an observation `y` (an observed wait time). `y` must depend on `rate`.", :dist-map {:rate "(dist/exponential 0.05)", :y "(dist/gaussian rate 1)"}, :task-id :wait-time, :eval-ok? true, :score-method "hmc", :observations {:y 3.4}, :obs-depends? true, :latent :rate, :variables [:rate :y], :seed 4244, :raw-text "rate ~ exponential(0.05)\ny ~ gaussian(rate, 1)", :code "(fn [trace]\n  (let [rate (trace :rate (dist/exponential 0.05))\n        y (trace :y (dist/gaussian rate 1))]\n    {:rate rate :y y}))", :parsed? true, :obs-addr :y, :fired? false} {:description "A radioactive sample emits particles; the time until the next emission is random. Write a model with a latent `rate` (the decay rate) and an observation `y` (an observed time). `y` must depend on `rate`.", :dist-map {:rate "(dist/exponential 1)", :y "(dist/exponential rate)"}, :task-id :decay-time, :eval-ok? true, :score-method "hmc", :observations {:y 1.8}, :obs-depends? true, :latent :rate, :variables [:rate :y], :seed 4245, :raw-text "rate ~ exponential(1)\ny ~ exponential(rate)", :code "(fn [trace]\n  (let [rate (trace :rate (dist/exponential 1))\n        y (trace :y (dist/exponential rate))]\n    {:rate rate :y y}))", :parsed? true, :obs-addr :y, :fired? false} {:description "A radioactive sample emits particles; the time until the next emission is random. Write a model with a latent `rate` (the decay rate) and an observation `y` (an observed time). `y` must depend on `rate`.", :dist-map {:rate "(dist/exponential 0.01)", :y "(dist/exponential rate)"}, :task-id :decay-time, :eval-ok? true, :score-method "hmc", :observations {:y 1.8}, :obs-depends? true, :latent :rate, :variables [:rate :y], :seed 4246, :raw-text "rate ~ exponential(0.01)\ny ~ exponential(rate)", :code "(fn [trace]\n  (let [rate (trace :rate (dist/exponential 0.01))\n        y (trace :y (dist/exponential rate))]\n    {:rate rate :y y}))", :parsed? true, :obs-addr :y, :fired? false} {:description "A radioactive sample emits particles; the time until the next emission is random. Write a model with a latent `rate` (the decay rate) and an observation `y` (an observed time). `y` must depend on `rate`.", :dist-map {:rate "(dist/exponential 1)", :y "(dist/exponential rate)"}, :task-id :decay-time, :eval-ok? true, :score-method "hmc", :observations {:y 1.8}, :obs-depends? true, :latent :rate, :variables [:rate :y], :seed 4247, :raw-text "rate ~ exponential(1)\ny ~ exponential(rate)", :code "(fn [trace]\n  (let [rate (trace :rate (dist/exponential 1))\n        y (trace :y (dist/exponential rate))]\n    {:rate rate :y y}))", :parsed? true, :obs-addr :y, :fired? false} {:description "A radioactive sample emits particles; the time until the next emission is random. Write a model with a latent `rate` (the decay rate) and an observation `y` (an observed time). `y` must depend on `rate`.", :dist-map {:rate "(dist/exponential 1)", :y "(dist/exponential rate)"}, :task-id :decay-time, :eval-ok? true, :score-method "hmc", :observations {:y 1.8}, :obs-depends? true, :latent :rate, :variables [:rate :y], :seed 4248, :raw-text "rate ~ exponential(1)\ny ~ exponential(rate)", :code "(fn [trace]\n  (let [rate (trace :rate (dist/exponential 1))\n        y (trace :y (dist/exponential rate))]\n    {:rate rate :y y}))", :parsed? true, :obs-addr :y, :fired? false} {:description "A radioactive sample emits particles; the time until the next emission is random. Write a model with a latent `rate` (the decay rate) and an observation `y` (an observed time). `y` must depend on `rate`.", :dist-map {:rate "(dist/exponential rate)", :y "(dist/exponential rate)"}, :task-id :decay-time, :eval-ok? false, :score-method nil, :observations {:y 1.8}, :obs-depends? true, :latent :rate, :variables [:rate :y], :seed 4249, :raw-text "rate ~ exponential(rate)\ny ~ exponential(rate)", :code "(fn [trace]\n  (let [rate (trace :rate (dist/exponential rate))\n        y (trace :y (dist/exponential rate))]\n    {:rate rate :y y}))", :parsed? true, :obs-addr :y, :fired? false}]
